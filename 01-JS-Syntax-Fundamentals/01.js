function solve(num) {
    console.log(num * 2);
}


function multiply(num) {
    let totalSum = 0;

    totalSum += (num * 2)

    return totalSum
}

function multiplyNumber(num) {
    return (num * 2)
}

