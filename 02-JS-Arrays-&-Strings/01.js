function solve(exampleArray) {
    let firstElement = exampleArray[0];
    let lastElement = exampleArray[exampleArray.length - 1];

    console.log(firstElement + lastElement);
}

// solve([1, 2, 3, 4]);